// Superseded. The receipt UI now lives in:
//   receipt-view.tsx   — the printable receipt
//   receipt-gate.tsx   — the guest phone-verification gate
//   print-button.tsx   — the print / save-as-PDF control
// page.tsx renders those directly. This file is no longer imported anywhere and
// is safe to delete (the shell was unavailable to remove it here).
export {};
